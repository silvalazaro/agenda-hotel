export interface Todo {
  id: number;
  content: string;
}

export interface Meta {
  totalCount: number;
}

export interface Usuario {
  id: number;
  nome: string;
  ativo: boolean;
  versao: number;
}
export interface FieldColumn{
  name: string;
  label: string;
}
